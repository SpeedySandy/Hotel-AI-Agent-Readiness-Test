# Morning briefing — 2026-06-10

## Top of mind
First run of the personal-OS briefing — proving the pipeline end-to-end. Most data sources are empty until you finish the one-time setup in `docs/setup.md`: iCloud credentials, the Mac Notes-export launchd job, and the scheduled trigger. Once those land, this briefing will be fully populated daily at 07:00 Europe/Berlin.

## Important emails
_None._

## Today's calendar
_None._

## Tomorrow at a glance
_None._

## Reminders
**Overdue**

_Reminders unavailable — set `ICLOUD_USERNAME` and `ICLOUD_APP_PASSWORD`._

**Due today**

_Reminders unavailable — set `ICLOUD_USERNAME` and `ICLOUD_APP_PASSWORD`._

## Notes highlights
_No snapshot found in `claude-notes-export/`._ Install the Mac launchd job from `docs/notes-export.applescript.md` so this section starts populating.

## Suggested captures
_None today._ (Once the inbox + Notes are populated, this section will list copy-pasteable `/capture …` lines drawn from action items.)

## Suggested plan
- **Now → +30 min** — Generate an iCloud app-specific password at appleid.apple.com → set `ICLOUD_USERNAME` and `ICLOUD_APP_PASSWORD` on this Claude Code on the web environment.
- **+30 → +60 min** — Install the Mac-side Notes-export launchd job from `docs/notes-export.applescript.md`. Verify `snapshot.md` appears in your Drive's `claude-notes-export/` folder.
- **+60 → +90 min** — Configure the scheduled trigger at code.claude.com (cron `0 7 * * 1-5`, branch `main`, prompt `Run /briefing`).
- **Anytime** — Try the new commands: `/capture <text>`, `/done <text>`, `/schedule <text>`, `/findtime <text>`, `/reply <text>`, `/note <text>`.

---

_Generated by the morning-briefing skill. Source: `.claude/skills/morning-briefing/SKILL.md`._
